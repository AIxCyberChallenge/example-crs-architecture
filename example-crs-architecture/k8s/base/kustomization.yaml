apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

secretGenerator:
- name: ghcr-creds
  type: kubernetes.io/dockerconfigjson
  files:
  - .dockerconfigjson
